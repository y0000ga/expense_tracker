import { NavigationContainer } from '@react-navigation/native'
import StackNavigator from '@/navigation/NativeStackNavigator'

const AppNavigator = () => (
  <NavigationContainer>
    <StackNavigator />
  </NavigationContainer>
)

export default AppNavigator
